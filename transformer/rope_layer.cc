#include "transformer/rope_layer.h"

namespace tlm {

RopeLayer::RopeLayer() = default;
RopeLayer::~RopeLayer() = default;

VectorView RopeLayer::Forward(VectorView input) {
  return {};
}

}  // namespace tlm
